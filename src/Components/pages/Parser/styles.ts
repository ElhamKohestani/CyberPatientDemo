import React from "react";



export const containerDiv: React.CSSProperties = {
    backgroundColor: "#D9D9D9",
    // This padding is ignore by the Tab component in parser page.
    padding: "6px",
    height:"80vh",
    borderRadius: "5px"
  };

  export const scrollableDiv : React.CSSProperties = {
    overflow: "auto"
  }

  export const colStyles : React.CSSProperties = {
    padding:"4px"
  }